name: Build and Release with PNPM

on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write
  actions: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 6.x # Omit or adjust based on your packageManager field

      - name: Install dependencies with pnpm
        run: pnpm install

      - name: Build the project
        run: pnpm run build

      - name: Compress build artifacts
        run: zip -r build-artifacts.zip ./path/to/your/build/directory

      - name: Get the release information
        id: get_release
        run: echo "::set-output name=upload_url::$(gh release view ${{ github.ref_name }} --json uploadUrl -q .uploadUrl)"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.get_release.outputs.upload_url }}
          asset_path: ./build-artifacts.zip
          asset_name: build-artifacts.zip
          asset_content_type: application/zip
